This is a public Repo that is used to combine Docker CLI with OpenClaw so it can be pulled into a container in one go.
The Action will automatically update the image whenever a new version of OpenClaw is released.
